# 3.0.4 shipped without required native binaries; 3.0.5 repairs the wheels.
pywinpty==3.0.5

# ui.web imports voice.call at boot, and the call pipeline resamples audio with
# numpy. Without it the frozen sidecar died on startup with an unhandled
# ModuleNotFoundError before the window ever appeared.
numpy>=2.1

# ZoneInfo on Windows has no zone database without this; the journal and
# the webhook ingress (which mounts its location route) both need one.
tzdata>=2024.1
